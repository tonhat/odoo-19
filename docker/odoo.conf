[options]
; Minimal container-friendly config
admin_passwd = admin
http_port = 8069

; Addons
addons_path = /opt/odoo/odoo/addons

; Persistent data (filestore, sessions)
data_dir = /var/lib/odoo

; Logging
log_level = info
